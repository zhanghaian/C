#include <stdio.h>

int main() {
    int n, k;
    scanf("%d %d", &n, &k);

    int a[1005];
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }

    int count = 0;
    for (int j = 0; j < n; j++) {
        
    }
    
    return 0;
}